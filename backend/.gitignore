# Dependencies
node_modules/

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Runtime uploads (avatars, message media, status files)
uploads/

# Logs
*.log
npm-debug.log*

# Coverage / test output
coverage/

# AI index build cache
scripts/.cache/
.cache/

# Generated AI indexes (ship .json.gz; plain .json is local build output)
data/ai/*.json
!data/ai/seed-smart-replies.json
!data/ai/seed-translate.json
!data/ai/curated-phrases.json
!data/ai/*.json.gz

# Legacy removed — do not re-add
data/translations.json
data/vega-dict.db
data/vega-dict.db-*
data/vega-dict.db-shm
data/vega-dict.db-wal
