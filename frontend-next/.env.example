NEXT_PUBLIC_API_URL=http://localhost:5500

# Optional: normally the frontend fetches ICE servers from the backend.
# Set relay only temporarily when QA-testing a TURN server.
# NEXT_PUBLIC_WEBRTC_ICE_TRANSPORT_POLICY=relay
NEXT_PUBLIC_API_URL=http://localhost:5500

# Optional: normally the frontend fetches ICE servers from the backend.
# Set relay only temporarily when QA-testing a TURN server.
# NEXT_PUBLIC_WEBRTC_ICE_TRANSPORT_POLICY=relay
