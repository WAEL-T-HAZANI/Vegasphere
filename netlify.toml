# Monorepo: build only the Next.js app in frontend-next.
# Do not set publish or command here — Netlify's Next.js runtime handles both.
[build]
  base = "frontend-next"

[build.environment]
  NODE_VERSION = "20"
